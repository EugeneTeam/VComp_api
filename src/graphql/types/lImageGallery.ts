import { gql } from 'apollo-server';

export default class LImageGallery {
    static resolver() {
        return {
            Query: {

            },
            Mutation: {

            },
        }
    }
    static typeDefs() {
        return gql``;
    }
}
